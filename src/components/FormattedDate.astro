---
import { formatFrenchDateTime, toDate } from "@/lib/date-format.mjs";

interface Props {
  date: Date | string;
}

const { date } = Astro.props as Props;
---

<time class="post-date" datetime={toDate(date).toISOString()}>
  {formatFrenchDateTime(date)}
</time>
